<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext for local development and trusted LAN ranges, not globally -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Localhost for emulator host 10.0.2.2 and device testing -->
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <!-- Private LAN ranges -->
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <!-- Tailscale CGNAT range 100.64.0.0/10 -->
        <domain includeSubdomains="true">100.64.0.0</domain>
        <!-- MagicDNS - allow .ts.net -->
        <domain includeSubdomains="true">ts.net</domain>
    </domain-config>
    <!-- Default deny cleartext for everything else, require TLS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
